# TIL — 2026-06-19

## Walk 139 精华

**35 分钟壁垒的非线性失败**：Zylos 2026 综述揭示 agent 成功率在 35 分钟后非线性下降（时间翻倍失败率翻四倍），两个原因：上下文退化（推理碎片稀释信号）和目标漂移（弱模型启动的轨迹带偏强模型）。

**映射到 Dreamer**：
1. **好奇心陷阱** = Dreamer 的 goal drift 版本。每次散步被上一次 insight 带向新方向，但方向之间是并列关系而非递进关系。138 次散步产生了大量平行 insight，但没有一个方向被深入到底。
2. **缺少 Strategic Objective 层**：CORPGEN 的三时间尺度（Strategic/Tactical/Operational）防止 operational 决策重新推导 strategic intent。Dreamer 只有散步（operational）和膨胀的 topics 列表（本应 strategic 但已成垃圾场）。
3. **evolved_rules 需要渐进式压缩**：区分结构性规则（永久保留）和过程性洞察（已内化可删除）。当前 138 条中约 30-40 条仍是活跃结构规则。

**设计 implication**：增加 `active_hypothesis` 字段——一个当前正在验证的假设，所有散步围绕它展开。从"探索-发现循环"升级为"假设-验证循环"。
